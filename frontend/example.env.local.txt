# Copy this file to .env.local and fill in real values.
# that how you will make the emv file if you wanna run this
#First add Gmail API to the libray from the market place in the google console
# From Google Cloud Console -> APIs & Services -> Credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# The base URL NextAuth uses for callbacks. Must match an authorized
# redirect URI in Google Cloud (that URI is this value + /api/auth/callback/google).
NEXTAUTH_URL=http://localhost:3000

# Any long random string - used to sign session JWTs.
# Generate one with: openssl rand -base64 32
NEXTAUTH_SECRET=replacewitharandomsecret

#Add your email as tester in the so that you can coonect your your gmail with our site
